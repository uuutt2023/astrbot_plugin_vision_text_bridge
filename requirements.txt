# AstrBot 内置 httpx (AsyncOpenAI 依赖), 通常无需额外安装
# 如果启动报 ModuleNotFoundError: No module named 'httpx', 装一下:
# httpx>=0.24

# No additional HTTP deps — main_server.py uses Python stdlib (asyncio.start_server)
